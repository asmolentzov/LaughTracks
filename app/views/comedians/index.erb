<h1>Comedians</h1>
<section class="row" id="statistics">
  <h2>Statistics</h2>
  Average Age: <%= @average_age %>
  Average TV Special Runtime: <%= @average_length %>
</section>
<section class="row">
    <% @comedians.each do |comedian| %>
      <article class="comedian" id=<%= comedian.name %>>
        <h3><%= comedian.name %></h3>
        <div class="comedian-stats">
          Age: <%= comedian.age %> <br />
          City: <%= comedian.city %> <br />
        </div>
        <div class="specials">
          <h6>Specials: </h6>
          <ul> 
            <% comedian.specials.each do |special| %>
            <li>
                <%= special.name %>
                Runtime: <%= special.length %>
                <img class="img-thumbnail" src="<%= special.image_location %>" alt="<%=special.name %>"
            </li>
            <% end %>
            </ul>
          </div>
      </article>
  <% end %>
</section>